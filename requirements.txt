PyQt6>=6.6
openai>=1.40.0
python-docx>=1.1.0
reportlab>=4.1.0
arabic-reshaper>=3.0.0
python-bidi>=0.4.2

# Streamlit web app and data
streamlit>=1.50.0
pandas>=2.3
numpy>=2.2
plotly>=5.18
requests>=2.31
openpyxl>=3.1.5
pytesseract>=0.3.10
easyocr>=1.7.1
docx2pdf>=0.1.8

# Security (for saving encrypted credentials)
cryptography>=45.0.0

# Browser automation (remember to install browser binaries after pip install)
playwright>=1.55.0

# Optional: enable daily auto-refresh if installed
streamlit-autorefresh>=1.0

# After installing requirements, run once to install Chromium binaries:
#   python -m playwright install chromium
